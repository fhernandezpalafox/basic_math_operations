name: basic_math_operations
description: Un paquete que provee funciones básicas para operaciones matemáticas.
version: 0.0.1
homepage: https://github.com/fhernandezpalafox/basic_math_operations

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
